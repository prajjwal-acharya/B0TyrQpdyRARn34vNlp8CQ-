#!/usr/bin/env bash
# Placeholder: model pulling is not needed for this project.
# Groq (Llama3) and Gemini are accessed via API — no local model download required.
#
# If you later add a locally-served model (e.g., via Ollama), add pull commands here.

echo "No local models to pull."
echo "Groq (Llama3) and Gemini are API-based — set GROQ_API_KEY and GEMINI_API_KEY in .env."
